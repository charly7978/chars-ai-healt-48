Voy a corregir esto desde la base de adquisición, sin simulación, sin datos aleatorios, sin fallback silencioso y sin permitir publicación biométrica cuando la cámara/flash/dedo no estén verificados.\n\nHallazgos críticos en el código actual\n- `usePPGMeasurement.start()` llama `cameraController.start()` después de varios `await`; en móviles esto puede romper el contexto de gesto del usuario para `getUserMedia`/torch. Esto explica síntomas como cámara sin captura real o flash que no prende.\n- `PPGCameraController.start()` hace “permission priming”, luego enumera, probea y vuelve a abrir streams. Eso es auditable, pero en móviles puede consumir el gesto, reiniciar cámara y fallar torch.\n- `acquisitionReady` existe, pero no se usa como bloqueo real del pipeline: `FrameSampler` empieza igual y `processFrame()` extrae/fusiona/detecta antes de que adquisición esté validada.\n- `FrameSampler` calcula `timestampMs` con `performance.now()` aunque rVFC entrega `now`; se pierde parte de la trazabilidad temporal real.\n- El fallback `fallback-any-video` puede abrir una cámara frontal o una cámara sin torch; está logueado, pero no es lo suficientemente estricto para un flujo forense.\n- `torchCondition = !camera.torchAvailable || camera.torchEnabled` permite publicar si el navegador dice “torch no disponible”. Para dedo con flash, eso debe bloquear o entrar en modo no-publicable, no “pasar”.\n- `FingerOpticalROI` sigue analizando el frame completo como ROI (`x:0,y:0,width,height`) y usa grilla 5x5; hay que reemplazarlo por ROI central/adaptativa real con exclusión de bordes.\n\nPlan de implementación\n\n1. Reescribir el arranque de cámara como adquisición atómica desde el botón Start\n- `measurement.start()` mantendrá la cadena directa del gesto del usuario.\n- El primer `getUserMedia` real se hará inmediatamente al comienzo del handler, sin probes previos que puedan romper el gesto.\n- El flujo será:\n  1. abrir cámara trasera con constraints estrictas;\n  2. asociar stream al `<video>`;\n  3. `video.play()`;\n  4. aplicar torch inmediatamente sobre el track activo;\n  5. leer capabilities/settings;\n  6. aplicar constraints finas en fases;\n  7. iniciar sampler;\n  8. recién después validar warmup y habilitar `acquisitionReady`.\n\n2. Eliminar fallback silencioso incompatible con PPG forense\n- No se publicará ninguna medición si:\n  - no hay cámara trasera verificada;\n  - el track resulta frontal/unknown sin evidencia suficiente;\n  - torch no está disponible, falla o el readback no confirma que quedó activo;\n  - el stream no entrega frames reales;\n  - FPS/jitter no pasan warmup.\n- `fallback-any-video` dejará de ser una ruta aceptable para medición. Si se usa solo para diagnóstico, quedará marcado como `acquisitionReady=false` y con razón explícita.\n\n3. Convertir `acquisitionReady` en compuerta dura\n- `usePPGMeasurement.processFrame()` no llamará a `RadiometricPPGExtractor`, `PPGChannelFusion`, `BeatDetector` ni `PPGPublicationGate` hasta que `camera.acquisitionReady === true`.\n- Durante warmup se publicará solo estado técnico: “CAMERA WARMUP / TORCH NOT VERIFIED / FPS UNSTABLE / WAITING REAL FRAMES”.\n- Ningún BPM, SpO2, latido, vibración ni waveform “REAL_PPG” podrá salir antes de esa compuerta.\n\n4. Agregar warmup real en `FrameSampler` + conexión con `PPGCameraController`\n- `FrameSampler` medirá frames reales con rVFC.\n- Usará `now` de `requestVideoFrameCallback` como timestamp principal; fallback: `performance.now()`.\n- Al acumular una ventana mínima, por ejemplo 45–60 frames con jitter aceptable, FPS medido > umbral y sin drops graves, llamará a un callback de readiness.\n- Si luego aparece freeze/jitter alto/drops, limpiará `acquisition con razones explícitas.\n\n5. Torch forense: verificación estricta y diagnóstico claro\n- `PPGCameraController` intentará torch con variantes conocidas:\n  - `{ advanced: [{ torch: true }] }`\n  - si aplica, `fillLightMode` cuando esté expuesto;\n  - reintentos cortos sobre el mismo track, sin reiniciar stream.\n- Readback estricto por `getSettings()`; si el navegador ignora la constraint, queda `torchStatus.resolved = \